import Layout from 'components/common/layout';
import React from 'react';

export const Blog = (): React.JSX.Element => (
  <Layout pageTitle="Blog Posts">
    <div className="text-center">Need to push some posts here...</div>
  </Layout>
);

export default Blog;
